# Shared Technical Terms

The Android build uses `jvmargs`.
The Apple project uses a `pbxproj` file.
The Flutter setup uses `flutterfire`.
The repository uses `gitmoji` for commit metadata.
The action policy uses `pinact` to verify immutable references.
The repository can use a linked `worktree`.
