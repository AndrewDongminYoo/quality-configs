# Add very_good_analysis to the consumer repository's dev_dependencies.
include: package:very_good_analysis/analysis_options.yaml

linter:
  rules:
    public_member_api_docs: false

formatter:
  trailing_commas: preserve
